{{#extend "layout"}}

{{#content "body"}}
<a href="/enrollments/create" class="btn btn-primary"> <i class="fa fa-plus"></i> New Enroll</a>
<br><br>
<table class="table table-bordered">
    <tr>
        <th>Course</th>
        <th>Topic</th>
        <th>Trainer</th>
        <th>Category</th>
        <th>Trainee</th>
        <th>Status</th>
        <th></th>
    </tr>
    {{#each enrollments}}
    <tr>
        <td>{{course.course_name}}</td>
        <td>{{topic.topic_name}}</td>
        <td>{{trainer.trainer_name}}</td>
        <td>{{trainer.category_id}}</td>
        <td>{{is_active}}</td>
        <td>
            <center>
                <a href="/enrollments/detail?course_id={{course.id}}&topic_id={{topic.id}}&trainer_id={{trainer.id}}"><i class="far fa-eye"></i></a>&nbsp;&nbsp;
                <a href="/enrollments/delete?course_id={{course.id}}&topic_id={{topic.id}}&trainer_id={{trainer.id}}"><i class="far fa-trash"></i></a>&nbsp;&nbsp;
                <a href="/enrollments/update?course_id={{course.id}}&topic_id={{topic.id}}&trainer_id={{trainer.id}}"><i class="far fa-pen"></i></a>&nbsp;&nbsp;
            </center>
        </td>
    </tr>

    {{/each}}
</table>
{{/content}}

{{/extend}}
